{"group":14,"indexes":[17],"awaitPromise":true,"length":712,"expression":"(async function(){const out=[];out.push({i:17,batch:\"batch-17\",value:await ((async()=>{\n      const b64=window.__b64buf;window.__b64buf='';\n      const bin=atob(b64);const arr=new Uint8Array(bin.length);\n      for(let i=0;i<bin.length;i++)arr[i]=bin.charCodeAt(i);\n      const fd=new FormData();\n      fd.append('file',new Blob([arr],{type:'image/webp'}),\"dashboard-web.webp\");\n      fd.append('title',\"Aero Suite — Painel operacional\");\n      fd.append('alt_text',\"Aero Suite — Painel operacional\");\n      const m=await wp.apiFetch({path:'/wp/v2/media',method:'POST',body:fd});\n      window.__asUrls[\"URL_DASHBOARD\"]=m.source_url;\n      return {key:\"URL_DASHBOARD\",url:m.source_url};\n    })())});return out;})()"}