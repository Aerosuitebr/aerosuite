# Redirecionamento canônico www → apex (aerosuite.com.br)
# Incluir no server block do host WordPress ou no proxy reverso.

server {
  listen 80;
  listen 443 ssl http2;
  server_name www.aerosuite.com.br;
  return 301 https://aerosuite.com.br$request_uri;
}
